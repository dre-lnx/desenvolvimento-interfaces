// Criando o contexto de tema (claro/escuro)
import { createContext } from "react";

// Criando um contexto para o tema da aplicação
const ThemeContext = createContext();

export default ThemeContext;
